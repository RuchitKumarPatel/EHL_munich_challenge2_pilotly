# Results

`policy_metrics.json` (full per-policy metrics, doubly-robust estimate,
segmentation, per-trajectory rows), `pareto.csv` / `pareto.png` (cost-quality
frontier), `ablation.json` (component ablation). `results/organizer/` mirrors the
same outputs for the organizer's raw export.

# Demo

Not built in this pass — project-method1/2's Streamlit demo pattern
(`streamlit>=1.30` optional dependency, `demo/app.py`) would slot in here
unmodified if wanted; the pipeline scripts and `results/policy_metrics.json` are
the actual deliverable this session focused on getting bug-free.

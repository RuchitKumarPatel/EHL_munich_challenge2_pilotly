# JOURNAL - one line per loop turn

turn 1 (merge): i0004 textclf negative result -- merged clean, FAILED data-safety on merged main (docs/POSTMORTEM-textclf.md quotes two concrete redaction serials), merge reverted, main green at f70bf1d. i0004 -> blocked with the exact fix. Nothing merged this turn.
- **turn 1** · `merge` · 01:19 · ok ($1.5363845000000003)

turn 2 (evaluate): i0001 router.verify -> ACCEPTED with two corrections. (1) NUMBERS.md needs no scanner -- report.py `_Q.__call__` already raises KeyError on an unknown key; measured 52 prose numerals, 0 orphans. (2) "resolve every numeral" is a vacuous test -- 28 claims carry small-int values 0..12, so README step numbers resolve by coincidence. Real scope is the 4 tracked artifacts (README, console.html, presentation template, site/index.html): 21 text-node numerals total once style/script are stripped. Non-obvious bit found: console.html is German, "11,4 pp" is refusal.mde_best_powered_arm_pair.pp with a decimal comma. i0003 left proposed (not a duplicate, not DEAD -- the hull/gap half is already in figs.py but the "beat it" half is live). No code written, no branch created.
- **turn 2** · `evaluate` · 01:24 · ok ($1.9266649999999998)
- **turn 4** · `review` · 01:46 · ok ($6.017702)

turn 5 (deck): built presentation.html at the repo root for the first time -- 6 slides, all 31 numerals interpolated from claims.json by key, frontier.png inlined as base64 (577 KB), team members left as a visible [FILL]. make all green, make test 82 OK, router.gates GREEN (4 pass / 2 warn / 0 fail). Two template captions corrected rather than shipped: the chart heading said "quality" (the outcome is process friction) and "held-out split" (the frontier is over all 1000 with out-of-fold scores). Headless render at 1920x1080: 0 px overflow on all 6 slides. SEPARATE FINDING: turn 4's i0007 containment did not hold -- the entire CLI reinstalls .git/hooks/pre-push at session start and this turn's first push shipped 4 more checkpoint refs (34 -> 36 on origin). Replaced with `entire configure --local --skip-push-sessions`, verified by a second push. i0007 stays blocked; the refs already on origin still need a human.
- **turn 5** · `deck` · 01:59 · ok ($3.2333584999999996)

turn 6 (implement): i0001 router.verify built on idea/router-verify (83118f2). Enforces the prose-to-claims contract on the five tracked user-facing artifacts -- 41 numerals bound, 0 orphans; exits 0/1/2 like router.gates. Turn 2's two corrections were both taken (NUMBERS.md not rescanned; the gate reports ORPHANS, not "every literal resolves to a key" -- ADR-009 says why). All four of turn 2's success criteria met, including the negative control in three forms. make test 93 OK (was 82), make all green, router.gates GREEN 4 pass / 2 warn / 0 fail. Scope was larger than turn 2 measured because turn 5 filled presentation.html: 36 live prose numerals there, not 0. FOUR REAL PARSER CASES the first run surfaced -- 3.6% / 41.0% / 92.0% are percent renderings of fraction claims and -3.83 is signed; a naive parser calls all four orphans. MEASURED LIMIT recorded rather than hidden: slipping the console MDE 11,4 -> 11,7 is NOT caught because 11.7 is recon.turns.per_line.claude-fable-5, so the negative control uses 11,9 and ADR-009 states the limit.

- **turn 6** · `implement` · 02:10 · ok ($3.5877975000000006)


turn 7 (implement): i0005 + i0011 on idea/data-safety-detectors -- both leak-detector gaps closed in one turn, as the turn-4 evaluate note asked. i0011: the concrete-placeholder pattern required a trailing `_<digits>`; it now takes any ATTACHED non-alphanumeric separator and normalises back, so the allowlist stays at 4 of its cap of 8. Whitespace is excluded and that was measured -- with it allowed the file failed on its own docstring. i0005: new RepoCarriesNoOpaqueExportToken asks the export whether it knows a token instead of matching a shape; 23 candidates over 114 files, 0 hits, base64 data URIs stripped first (presentation.html's inlined PNG otherwise contributes ~9000 same-shape fragments), the 9 arm ids exempt structurally. New ConcretePlaceholderShapes tests the pattern itself, not the corpus -- that distinction is the whole finding, since a corpus test is green both when the repo is clean and when the detector is blind. Both corpus checks confirmed against a leak seeded into an untracked scratch file; failure output prints a masked shape, never the token. make test 92 OK, make all green, router.gates GREEN (4 pass / 2 warn / 0 fail). ADR-010. TWO THINGS THE MERGE TURN SHOULD KNOW: (1) i0001 is already implemented on idea/router-verify (commit 83118f2) from turn 6 -- main's copy of the backlog still says accepted because the flag was set on that branch. (2) The new detector caught this turn's own first backlog note, which had quoted the synthetic probe token from its own finding -- the third instance of that exact failure mode, and the first one caught by a test instead of by a human. Note rewritten to describe the probe without naming it; the offending commit was reset before anything was pushed.
- **turn 7** · `implement` · 02:25 · ok ($3.560496)

turn 7b (implement): i0009 + i0010 + i0013 on idea/push-gate-per-ref -- the whole push-gate family in one commit, since they are one mechanism and the turn-4 notes asked for i0010 and i0013 together. New loop/push_gate.sh is the single implementation of "may this ref leave this machine"; run.sh and bootstrap.sh source it and neither contains a git push of its own. New DATA_SAFETY_SCAN_REF makes the two repo-file checks enumerate git ls-tree and read blobs with git cat-file, so each branch is scanned against ITS OWN tree before that branch is pushed -- a failing branch is skipped and named, the clean ones still go. THE MEASUREMENT: run against this repo's five real branches, the per-ref scan is red on text-classifier-finetune (naming docs/POSTMORTEM-textclf.md) and green on the other four -- that is the branch the old gate pushed unscanned whenever main was checked out, so the bug is confirmed by the fix catching a real instance rather than only a synthetic one. Behavioural test drives push_all_branches against a throwaway repo with a real local remote and asserts what LANDED, not what was printed; mutating the per-ref check to a no-op kills 3 of 4. The dirty probe ref is a git commit-tree commit left dangling on purpose, so no push path can see it. bootstrap.sh also reordered: make all before make test, or the two artifact-scanning classes SkipTest on an empty directory and are green for the wrong reason. i0013's docs restated, not deleted -- both files now name entire/* and the merge/deck prompt pushes as paths the gate does NOT cover. make test 93 OK (was 82), make all green, router.gates GREEN 4 pass / 2 warn / 0 fail. ADR-011. NOTE FOR THE MERGE TURN: three branches are now waiting -- idea/router-verify (i0001), idea/data-safety-detectors (i0005+i0011) and this one -- and all three touch tests/test_data_safety.py or docs/DECISIONS.md, so expect ADR ordering and one test-file conflict. This branch's change to that file is confined to _repo_files, a new _repo_blob helper and the two call sites.
- **turn 7** · `implement` · 02:37 · ok ($4.545730000000001)

turn 7c (implement): i0008 on idea/supervisor-untrusted-state -- the supervisor's budget check interpolated cost_usd out of loop/state/state.json into a `python -c` source string, and that file is rewritten by every turn agent running under bypassPermissions. THE MEASUREMENT: one probe payload, both paths. Through the old interpolated form it executes and writes its sentinel file; through stop_reason as it now stands the sentinel is absent and the loop stops with "unreadable cost_usd". 4 of the new file's 9 tests failed before the fix, 9 pass after. Converted all five readers rather than only the reachable one -- state_field, usage_pct, quota_reset_epoch, is_rate_limited, the per-turn COST extractor -- each single-quoted with values passed as argv. TWO THINGS FOUND WHILE FIXING IT: the max-turns guard was switchable off by writing a non-integer turn counter, because `[ "$turn" -ge 40 ]` errors on one and an erroring guard reads as "do not stop" (new state_int coerces it); and the budget check is three-valued now (over / under / unreadable) and fails CLOSED on the third, which is the value a merely corrupt state file reaches by accident. The durable guard is textual, so it keeps holding when someone adds a sixth reader: no `python -c` argument in run.sh may be double-quoted, and none may continue past its closing quote. run.sh gained a LOOP_RUN_SOURCED guard so the helpers can be sourced without starting a night. make test 91 OK (was 82), make all green, router.gates GREEN 4 pass / 2 warn / 0 fail. ADR-012. FOR THE MERGE TURN: four branches now wait -- idea/router-verify (i0001), idea/data-safety-detectors (i0005+i0011), idea/push-gate-per-ref (i0009+i0010+i0013) and this one. All four append to docs/DECISIONS.md, so expect an ADR ordering conflict there; this branch is ADR-012 and touches no test file the others touch. It DOES rewrite loop/run.sh's helper block, and idea/push-gate-per-ref rewrites the same file's push block -- different regions, but check the merge rather than trusting it.
- **turn 7** · `implement` · 02:48 · ok ($3.4051245000000003)

turn 7d (implement): i0012 plan_turn cadence -- IMPLEMENTED on idea/plan-turn-cadence. Four idea branches were already sitting unmerged (i0001, i0005/i0011, i0008, i0009/i0010/i0013), so this turn took the one accepted idea nobody had claimed. Repro first, as a failing test: over turns 1..40 with a non-empty queue, `review` fired 0 times for (5,10), (4,4), (5,5) and (3,6), and the shipped (5,4) fired 8 of its 10 multiples. The naive fix -- swap the two checks -- was written, measured and REJECTED in the same turn: it starves the deck instead, 0 deck turns for (4,4) and (5,5). Shipped rule is review-wins-the-tie plus a one-turn deck deferral. Full grid deck 1..12 x review 1..12, turns 1..40: 0 review multiples missed, deck staleness bounded at deck_every+1. Both cadences now share one guard so 0 disables either instead of raising. make test 96 OK, make all green, router.gates GREEN (4/2/0). ADR-013.
- **turn 7** · `implement` · 02:56 · ok ($2.6624209999999997)

turn 7e (implement): i0004 -- the textclf negative result is now landable. Every other accepted idea was already built by a parallel session on its own branch (i0001 router-verify, i0005/i0011 data-safety-detectors, i0008 supervisor-untrusted-state, i0009/i0010/i0013 push-gate-per-ref, i0012 plan-turn-cadence), so i0004 was the one accepted idea left with a brief and no code. Merged main into text-classifier-finetune first, watched tests.test_data_safety fail on the merged result exactly as turn 1 recorded, then rewrote section 5's Risk paragraph to name the SHAPE of both problems rather than quote the five tokens proving them. Swept every file the branch adds against the export with the opaque-token heuristic: 3 candidates, 0 hits. make test 82 OK, make all green (301 claims), router.gates GREEN 4/2/0. No headline number touched. The branch is now a fast-forward of main. docs/DATA-SAFETY-DEBT.md item 1 restated: the tree is clean, the pushed history is not, and 24e7469 on origin is still i0006 and still a human's call.
- **turn 7** · `implement` · 03:02 · ok ($1.6899454999999999)

turn 7f (implement): no accepted idea was left unbuilt -- five parallel turn-6/7 sessions had already built all nine (i0001, i0004, i0005, i0008, i0009, i0010, i0011, i0012, i0013), each on its own branch, and each had appended to docs/DECISIONS.md, docs/IDEAS.md and loop/JOURNAL.md. So the build work this turn was the integration: idea/integrate-turn7 merges all six branches. Verified each green in isolation first, then merged in dependency order. make test 140 OK (main is 82), make all green (301 claims), router.gates GREEN 4 pass / 2 warn / 0 fail, router.verify PASS on 41 numerals. ADR-008..013 needed no renumbering; the parallel sessions had coordinated. backlog.json auto-merged correctly and was not hand-edited. ONE REAL DEFECT, invisible to every branch alone: i0009 made the repo-file ENUMERATION ref-aware and i0005 added an opaque-token detector that reuses that enumerator but read every path back out of the WORKING TREE, so loop/push_gate.sh -- which already runs the whole module per ref -- scanned main's files and called every branch clean. Fixed by reading through _repo_blob; test written first (tests/test_push_gate.py::OpaqueTokenScanIsRefAware, dangling commit, invented probe, both directions, probe asserted absent from the export) and watched to fail. THE MERGE TURN SHOULD MERGE idea/integrate-turn7, not the six branches.
- **turn 7** · `implement` · 03:19 · ok ($4.953749499999999)

turn 8 (review): scoped to c05e078..main (presentation.html, first review of the deck) and the unmerged idea/integrate-turn7 (+2898/-156). 9 findings filed, 7 accepted. Headline: the deck slide-6 number +4.59 pp is upper(A) - upper(B) and bounds nothing, and the router moves 3.6% of spend against a hull at -3.83 pp, so it cannot reach it by arithmetic -- verified against router/ope.py:594 and claims.json, not taken on the agent's word (i0021). Two controls proven inert: the per-ref data-safety scan exits 0 with 6 skips on an unresolvable ref, so any git error reads as "branch clean" (i0022); and the new opaque-token detector needs 16 chars while the three raw ids it was written for are 11, so it matches none of the five tokens commit af4e78b removed (i0018, with the candidate-count curve 27/63/93 measured so the fix is one line). i0023 (option-shaped ref name reaching git push) traced back to main -- pre-existing, must not block the merge. Re-verified the branch independently: 140 tests, gates GREEN, verify PASS on 41 numerals, textclf isolated from make all. Nothing fixed this turn; no dataset content found in the diff.
- **turn 8** · `review` · 03:43 · ok
- **turn 8** · `review` · 03:43 · ok ($13.04013965)

turn 9 (merge): i0017 MERGED to main as c38d1cd -- the six turn-6/7 branches, and with them i0001, i0004, i0005, i0008, i0009, i0010, i0011, i0012, i0013, all now merged. Gate run twice, once on the branch before merging and once on the merged result, both green: make test 140 OK (main was 82), make all 301 claims, router.gates GREEN 4 pass / 2 warn / 0 fail, router.verify PASS on 41 numerals across 5 artifacts. i0004's acceptance criterion checked directly rather than taken from the note -- nothing outside router/textclf references it and importing report/gates/ope leaves torch and transformers unloaded. main was 1 ahead of origin/main, no divergence, no teammate push. THE ONE JUDGEMENT CALL: i0017's note said take the BRANCH side on the backlog.json conflict, and that would have been wrong -- the branch was cut before turn 8, so its backlog stops at i0017 and taking it wholesale would have silently deleted i0018..i0026, including the deck finding i0021. Resolved as a union instead (branch flags for i0001..i0017, note-union on i0017, main's turn-8 entries appended), verified id-by-id before committing. JOURNAL unioned chronologically, IDEAS.md regenerated rather than hand-merged. All three conflicts were loop bookkeeping; zero source conflicts. Push shipped no checkpoint refs -- turn 5's `entire configure --skip-push-sessions` containment is still holding. NEXT: i0022 and i0018 first, per turn 8 -- they are the two that make a green data-safety gate mean less than it reads -- then i0021 before the deck is shown.
- **turn 9** · `merge` · 03:51 · ok ($1.8825195)

turn 10 (deck): the deck rebuilt from current claims, and i0021 fixed at source rather than on the slide. Nothing in the numbers moved -- `make all` reproduced 301 claims and every acceptance value in router.figs is unchanged -- so this turn's work is entirely the correction the turn-8 review demanded. THE DEFECT: both frontier legs come from `router.ope.non_inferiority_bound`, which returns `(None, None, hi, diag)` -- a one-sided UPPER bound and nothing else -- so `upper(router) - upper(mixture)` was the deck's largest number and bounded nothing in either direction. FIXED IN FOUR PLACES so no reader can reach the bad inference: the claims key is renamed `frontier.gated_router.upper_bound_minus_hull_upper_bound_pp` (value unchanged, only two call sites existed); NUMBERS #5 states both levels and says do not subtract them; the chart's double-headed arrow is labelled with the two bounds instead of the distance between them; and the deck quotes no difference at all. WHAT REPLACES IT is stronger and was the point: the identified delta is a spend-weighted mean of per-run differences in [0,1] over switched runs only, so its magnitude cannot exceed the share of spend the policy moves -- 3.6% here, against a hull at -3.83 pp. No policy at this tau reaches that hull however well it routes, so the finding is about a knob we chose, not about a rule the data rejected. `policy_point` now carries `switched_spend_share` on every plotted point and it is a new frontier CSV column, so the ceiling is a property of each point; computed there it is 0.035888244943512816, equal to `ope.routed.switched.spend_share` from the independent OPE path -- two code paths, all digits. Also corrected: the comparator at $375.45 is an interpolation between claude-sonnet-5 and claude-opus-5, i.e. a RANDOMISATION with roughly 40% of its weight on the most expensive policy on the chart, so "one cheap arm beats our router" was wrong twice. NO gap number was produced, deliberately -- ADR-014 records that it needs a joint job-clustered bootstrap evaluating both policies inside one replicate, and that the "4.59 < 11.4 MDE" argument is a different estimand and must not be used. TWO THINGS THE HEADLESS RENDER CAUGHT that a read of the markup would not: slide 5 overflowed 1080p by 30 px once the corrected wording landed (fixed by trimming and a tighter h2 margin, re-measured to 0 on all six), and the first version of the chart's FINDING block ran off the right edge of the figure and its third line collided with a point label. Both were only visible in a screenshot. Gate: make test 140 OK, make all green (301 claims), router.gates GREEN 4 pass / 2 warn / 0 fail, router.verify PASS on 44 numerals across 5 artifacts, tests.test_data_safety 21 OK before the push. i0021 merged; i0003 re-scoped by note rather than closed, because its stated success criterion ("the gap quantified and a claims key for it") is now the very thing ADR-014 forbids. STILL BLOCKED FOR A HUMAN: i0014 -- TEAM_MEMBERS is empty and slide 1 still reads "Team members: [FILL]". That is a 30-second fix in the morning and it is the last placeholder on the deck.
- **turn 10** · `deck` · 04:12 · ok ($7.547360999999998)

turn 14 (implement): i0025 -- the prose gate's stripper -- IMPLEMENTED on idea/verify-prose-stripper (d9f4201), ADR-020. THE ACCEPTED QUEUE WAS EMPTY: all seven of i0015/i0018/i0019/i0022/i0023/i0024/i0026 are built and already integrated onto idea/integrate-turn12, and no evaluate turn has run since turn 8. Of the four remaining proposed entries, i0025 was the only one whose note carried reproductions rather than a hypothesis, so it is the only one an implement turn could take without guessing at scope. ALL THREE REPRODUCED FIRST, red watched -- 4 failures plus 1 error against the pre-fix module, 24 green after. (1) `_FENCE_RE` matched only a CLOSED fence pair, so a dropped closing ``` left the fenced command in the scanned prose; since 28 claims carry small-int values 0..12, a shell flag mis-binds as easily as it orphans. Blanked to EOF now, mirroring the open-`<script>` fallback the HTML path already had. (2) `<[^>]+>` closes at the FIRST `>`, including one inside a quoted attribute value, so the tail of such a tag was read as prose. The rule is quote-aware now, and its three alternatives are disjoint on their first character, so it stays linear rather than trading a correctness bug for backtracking. (3) The `%` that licenses reading a numeral as a percent rendering of a fraction claim was found by slicing a fixed two characters after the literal, so two spaces before the sign silently disabled the fraction path and a legitimate percent reported as an orphan. THE RESTRICTION IS THE LOAD-BEARING HALF, not the widening: only WHITESPACE may sit between, and there is a test in the opposite direction, because a lookahead that scanned past words would let the tolerance bind numerals that are not shares and the gate would quietly stop finding orphans at all. ONE JUDGEMENT CALL BEYOND THE BRIEF, and it cuts against the fix: blanking a dangling fence to EOF can HIDE a real numeral written after it -- a fail turned into a quiet pass, which is exactly the class ADR-015 and ADR-018 exist to refuse. So `run()` records the line and the report prints `WARNING - <file>:<line> unclosed fence`. Warning and not failure because a dropped ``` still renders as a page a human reads, whereas an unclosed `<script>` is a browser-visible breakage nobody can miss; escalating it to BLOCKED is the right end state but belongs with ADR-018's `Report.missing` machinery, which lives on the unmerged integration branch, so it is FILED rather than built -- building a second exit-2 path from main would have been a conflict in the same function for no gain. THE HONEST HEADLINE IS THAT THIS BUYS NOTHING TODAY: all three defects are latent against the five shipped artifacts, and that is measured rather than repeated from the turn-8 note -- substituting the quote-aware tag rule changes 0 lines across all four HTML artifacts, README.md carries 0 unclosed and 0 indented fences, and old parser and new give the IDENTICAL result on the shipped tree (44 bindings, 0 orphans, 0 warnings). A test re-measures the tag half on every run, so "this is hardening" cannot rot into a false claim the way a note can. What it buys is tomorrow: these fire the first time a human edits the deck or the README, and i0014 guarantees a human edits slide 1 in the morning. GATE: make test 153 OK (main is 140), make all green (301 claims, AUPRC unchanged at 0.4159785566844733), router.gates GREEN 4 pass / 2 warn / 0 fail, router.verify PASS 44 numerals across 5 artifacts exit 0, tests.test_data_safety 21 OK. No claims key touched, so no number-verifier; no feature or join touched, so no leak-hunter. TWO THINGS FOR THE MERGE TURN, both on the note for i0025. (a) ID COLLISION: `backlog.py add` minted the follow-up as i0027 because main's backlog stops at i0026, and idea/integrate-turn12 already holds a DIFFERENT i0027. The CLI has no rename and the state files must not be hand-edited, so it is recorded on both entries instead -- renumber THIS branch's i0027 (the unclosed-fence one) during the union. (b) idea/integrate-turn12's ADR-018 edits router/verify.py's ARTIFACTS tuple, `Report.ok`, `run()` and `main()`; this branch edits the strippers, `scan_artifact`, one added Report field and one added line in `run()`. Disjoint except those two spots -- take BOTH sides. ADR numbering was checked against that branch first: it holds 015..019, this is 020.

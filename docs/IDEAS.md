# IDEAS — the overnight loop's backlog

Generated by `loop/backlog.py render`. Do not edit by hand; edit the JSON via the CLI.

Turn: **1** · spend so far: **$0.00** · last turn type: **—**

## proposed (2)

### i0001 — Build router.verify: check every quoted number against claims.json

*code* — Evaluation rigor. results/NUMBERS.md already claims 'what router.verify checks prose against', but router/verify.py does not exist — the prose-to-claims contract is asserted, not enforced. Success = a module that scans NUMBERS.md, README.md and presentation.html, resolves every numeric literal to a stable claims.json key, and exits non-zero on any orphan.

### i0003 — Close out the single-arm mixture problem — beat it or headline the negative result

*evaluation* — Evaluation rigor and the off-policy special prize. NUMBERS.md #5: at equal spend the single-arm mixture frontier reaches -3.83 pp while the gated router reaches +0.76 pp, a 4.59 pp gap the wrong way. Either a gating rule closes it, or the honest headline of this project is the negative result, cleanly stated. Success = the frontier figure plots both curves with the gap quantified and a claims key for it.

## rejected (1)

### i0002 — Text classifier on the first user message as a routing signal · `text-classifier-finetune`

*analysis* — Routing insight. The whole multivariate fit buys only 0.0685 AUPRC over ctx_lines used raw (0.3475 -> 0.4160), which is a thin margin to build a router on. A hand-rolled text classifier over the pre-treatment system+first-user text may carry signal the count features miss. Success = out-of-fold AUPRC above 0.4160 under job-blocked folds, using pre-treatment columns only and excluding the two outcome-derived columns.

- turn 0: Already executed by a parallel session on branch text-classifier-finetune (commit 24e7469): ModernBERT fine-tune, frozen-encoder probe, arm-interaction head. Outcome NEGATIVE and adopted nothing — ADR-008 plus docs/POSTMORTEM-textclf.md. corr(Delta_hat, real stratum gap) = +0.042 against a design-based sonnet-opus contrast of +6.8pp stratum-adjusted. Do NOT rebuild this. See i0004 for landing the write-up.

## blocked (1)

### i0004 — Land the textclf negative result and its postmortem on main · `text-classifier-finetune`

*evaluation* — Evaluation rigor, and the challenge explicitly scores naming where an estimate fails. A measured negative result with a postmortem and an ADR is presentable evidence; leaving it on an unmerged branch wastes it. Success = ADR-008 and docs/POSTMORTEM-textclf.md on main with the pipeline still green, and no torch import reachable from make all.

- turn 0: Seeded as implemented so a merge turn verifies the gate on it rather than rebuilding it. The merge turn must NOT trust this flag — run make test, make all and router.gates on the branch first, and check that make all does not require torch.
- turn 1: Merge turn 1: merged CLEAN into main (no conflicts), but tests.test_data_safety.RepoSourceCarriesNoExportContent.test_no_undocumented_placeholder_in_any_repo_file FAILED on the merged result -- 81/82 pass, 1 failure. Merge reverted; main is back at f70bf1d and green (82 OK). Sole offender: docs/POSTMORTEM-textclf.md line 114 quotes the concrete placeholders PII_PERSON_7 and PII_PERSON_12 (the sentence explaining that redaction serials are renumbered per request). Fix is small and belongs to an implement turn -- EITHER rewrite line 114 to name the pattern generically (PII_PERSON_N / PII_PERSON_M) rather than two concrete serials, which is preferred because test_the_allowlist_stays_small caps the DOCUMENTARY allowlist, OR add both tokens to DOCUMENTARY in tests/test_data_safety.py with the renumbering finding as justification. Nothing else on the branch is wrong: the .entire/metadata offenders seen when testing the branch in isolation are an artifact of the branch base 0aa9d24 predating the tracked .entire/.gitignore that landed on main in f70bf1d -- they vanish after the merge. Branch is pushed and intact at 24e7469.
